
## Setup
Install dependencies:
```shell
$ bundle install
```

Create `example/config/database.yml`:
```shell
$ bundle exec rake db:setup
```

## Usage
Run example program:
```shell
$ bundle exec rackup
```
